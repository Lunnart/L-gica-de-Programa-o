alert("Olá Mundo!") // Aspas no que vai dentro do parenteses-SEMPRE!!!

let nome = "Nayara"; //Declarar variável com o seu nome

let comidaFavorita; //Declarar variável com a sua comida favorita e não atribuir valor

comidaFavorita = "Mucunzá Salgado"; //Atribuir valor a sua comida favorita

console.log(comidaFavorita); //Resposta do console é Mucunzá Salgado, pois foi atribuído valor a variável comidaFavorita antes de ser chamada no console.log

confirm.log(nome); //Resposta do console.log é undefined, pois não foi atribuído valor a variável nome antes de ser chamada no console.log, neste caso vai dá erro na resposta do console



nome = Júlio //A variável nome foi declarada como Nayara, mas depois foi atribuído o valor Júlio, ou seja, o valor da variável nome foi alterado para Júlio.

nome = prompt('Digite seu nome'); //A variável nome foi declarada como Nayara, mas depois foi atribuído o valor do prompt, ou seja, o valor da variável nome foi alterado para o que o usuário digitou no prompt.   

console.log(nome); //Resposta do console é o que o usuário digitou no prompt, ou seja, o valor da variável nome foi alterado para o que o usuário digitou no prompt.

// string = texto.

nome = "Nayara" //A variável nome foi declarada como Nayara.

 let idade = "29" //A variável idade foi declarada como 29, mas como é texto, o valor dela é string.

 console.log(typeof(idade)); //Resposta do console é string, pois a variável idade foi declarada como texto.